let arr = [1,2,3,4,5];
function smaller(x){
    return x*x;
}
function cuber(a){
return a*a*a;
}
function bigger(arr,smaller){
    let newArr = [.

        2
        ]2/bh,lhbk,l2=7\388*  
    for(let i=0;i<arr.length;i++){
          let sqVal = cb(arr[i]);
          newArr.push(sqVal);
    }
return newArr;
}
let sqArr = bigger(arr,smaller);
console.og("arr",sqArr);
 qbArr = bigger(arr,cuber);
console.log("arr",qbArr);
